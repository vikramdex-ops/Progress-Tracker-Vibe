name: Build, Release & Deploy

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write

# Only allow one deployment at a time
concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    environment: Production
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Type check
        run: bun tsc -b --noEmit

      - name: Validate environment
        run: |
          if [ -z "$VITE_API_URL" ]; then
            echo "ERROR: VITE_API_URL is not configured. Check GitHub Settings → Variables → Production"
            exit 1
          fi
          echo "✅ API URL configured: $VITE_API_URL"
        env:
          VITE_API_URL: ${{ vars.API_BASE_URL }}

      - name: Build
        run: bun run build
        env:
          VITE_API_URL: ${{ vars.API_BASE_URL }}

      - name: Upload artifact for Pages
        uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy-pages:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

  create-release:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Download build artifact
        uses: actions/download-artifact@v4
        with:
          name: github-pages
          path: dist

      - name: Package build output
        run: |
          cd dist
          zip -r ../progress-tracker-${{ github.run_number }}.zip .

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: build-${{ github.run_number }}
          name: "Progress Tracker — Build #${{ github.run_number }}"
          body: |
            ## Progress Tracker — Production Build

            **Commit:** ${{ github.sha }}
            **Workflow Run:** #${{ github.run_number }}
            **Live Site:** ${{ github.server_url }}/${{ github.repository }}

            ### What's Included
            - Complete frontend (React + Vite)
            - Gamification (XP, badges, streaks, levels)
            - Team lead dashboard with entry management
            - Employee dashboard with EOD submission
            - Light/dark mode, responsive design

            ### Deployment
            - **Frontend:** GitHub Pages (auto-deployed)
            - **API:** Deploy `api/` folder to Vercel
            - **Backend:** Airtable (free tier)

            ### Environment Variables (Vercel)
            ```
            AIRTABLE_PAT=your_pat_here
            AIRTABLE_BASE_ID=appXXXXXXXXXX
            ```
          files: progress-tracker-${{ github.run_number }}.zip
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
